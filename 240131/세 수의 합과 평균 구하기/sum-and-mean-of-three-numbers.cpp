#include <iostream>
using namespace std;

int main() {
    int a, b, c;
    cin >> a >> b >> c;
    int sum, mean ;
    sum = a + b + c;
    mean = sum /3;
    
    cout << sum << endl << mean ;

    return 0;
}